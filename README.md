# Test task

 - Vue 3 SPA
 - Nginx
 - Rick & Morty API

## Setup 
`docker-compose up`

[links](http://localhost)

### Dev env (node Vite dev ws)
`docker-compose -f docker-compose.dev.yaml up --build`
